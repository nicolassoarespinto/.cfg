[project]
name = "ssh-yank"
version = "0.1.0"
description = "Copy file contents to SSH client clipboard via OSC 52"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
ssh-yank = "ssh_yank:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
